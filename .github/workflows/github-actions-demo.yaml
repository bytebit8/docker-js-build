name: workflow_demo

on:
  push:
    branches: [main]

env:
  job_env: job_env1

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: print_job1_env1
        env:
          job1_env1: ${{ env.job_env }}
        run: echo "${{ env.job1_env1 }}"
