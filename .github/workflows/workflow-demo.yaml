name: workflow-demo

on: pull_request

jobs:
  job:
    runs-on: aliyun
    container:
      image: docker:dind
    steps:
      - name: Docker Pull
        run: docker pull nginx:latest
      - name: Docker List
        run: docker ps