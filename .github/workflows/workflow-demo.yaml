name: Workflow Demo

on: pull_request

jobs:
  job:
    runs-on: aliyun
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: password
    container:
      image: ubuntu:latest
    steps:
      - run: cat /etc/os-release
