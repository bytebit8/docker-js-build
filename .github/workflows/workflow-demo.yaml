name: workflow-demo

on: pull_request

jobs:
  job:
    runs-on: aliyun
    container:
      image: ubuntu:latest
    steps:
      - name: greeting
        run: echo "Hello, World!"
      - name: ls
        run: |
          ls /_work
          ls /_work/_temp


